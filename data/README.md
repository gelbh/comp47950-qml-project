# Data

Dataset and derived files for the COMP47950 QML project.

- **`raw/`** — Downloaded or original datasets.
- **`processed/`** — Train/test splits, scaled features, or other prepared data.

Large data files (e.g. CSV, Parquet) are gitignored; add paths or use a data registry if you need to share them.
